apiVersion: v1
data:
  tls.crt: "{{ sealed_secrets_public_key }}"
  tls.key: "{{ sealed_secrets_private_key }}"
kind: Secret
metadata:
  labels:
    sealedsecrets.bitnami.com/sealed-secrets-key: active
  name: masterloft-sealed-secrets
  namespace: sealed-secrets
type: kubernetes.io/tls


